[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pidbox"
version = "0.1.0"
description = "pidbox package"
readme = "README.md"
requires-python = ">=3.8"
